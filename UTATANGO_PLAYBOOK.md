# UtaTango 导演手册 (Director's Playbook)

本手册是 UtaTango 项目的“核心宪法”。
未来我们在为您添加任何新日语歌词时，AI 助手都**必须且只能**遵循本手册的指引进行即兴开发。

## 1. 创意宣言 (Improvisation Manifesto)
这是一场受《爱、死亡与机器人》（Love, Death & Robots）启发的“一歌一世界”网页即兴实验。
- **绝对独立**：每首歌的页面（位于 `src/pages/` 下的各个单曲组件）都是一个独立的沙盒！
- **杜绝模板**：**绝对不要**生搬硬套或复制其他单曲的 CSS 和交互结构。每一页都必须拥有为其情绪量身定制的全新配色、独特的交互特效（如画布粒子、动画滤镜）和全新的布局。
- **完全天马行空**：不要写千篇一律的背词卡片。不局限于专属小游戏，也可以是极具视觉冲击的特殊排版、打破常规的文字特效、甚至是一段纯视觉的交互式微电影。总之，风格、形式、体验必须完全不设限！项目的核心目的是为了给 USER 在记忆日语单词时提供持续的新鲜感，让每一次生成新页面都是一次“开盲盒”般的巨大惊喜。

## 2. 四大核心功能基线 (Mandatory Base Features)
在无限脑洞的创意之下，每一页都**必须**通过代码无缝实现以下四项基础功能，保证日语学习体验不降级：

1. **带注音的交互歌词系统**：
   - 包含日语汉字、罗马音（Romaji）和翻译。
   - 必须使用 HTML `<ruby>` 和 `<rt>` 标签为汉字上方悬浮渲染精准的振假名（Furigana）。
2. **原生发音朗读 (Web Speech API)**：
   - 每行歌词或独立单词必须可以通过点击事件触发浏览器的 `window.speechSynthesis` 朗读，使用高质量的 `ja-JP` 语言发音。
3. **核心单词弹出与互动**：
   - 提取歌词中 10-15 个 N5-N1 级别的重点单词。在歌词面板中为它们打上发光或特殊下划线。
   - 用户点击单词时，弹出一个设计完美、符合当前视觉主题的精美解释弹窗（释义、词性、发音）。
4. **无缝的主题化退出按钮**：
   - 页面左上角或合适位置，必须设计一个优雅的“返回主页大厅”按钮，将用户安全送回画廊。

## 3. 标准开发流与指引 (Workflow)
每次增加新歌时，AI 必须严格遵循以下流程：

### 阶段一：信息收集与确认
USER 需要提供以下三项核心信息：
1. **专辑名 (Album)**：如果是现有专辑，则追加；如果是新专辑，则创建新专辑层级。
2. **歌曲名 (Song Title)**
3. **完整歌词 (Lyrics)**
*(🔥 警告：如果 USER 缺少提供上述任何一项，AI 必须主动打断开发流程，并提醒 USER 补充完整后再进行后续设计！)*

### 阶段二：设计与开发
1. 阅读歌词，分析情绪（Vibe）。
2. 在 `src/styles/` 中建立其专属的 `.css` 文件（如 `shikaki.css`），不使用任何全局框架限制自己，手写最纯正的原生 CSS 特效、玻璃拟态、粒子悬浮等。
3. 将歌曲组件加入 `src/songsConfig.js` 的注册表中。
4. 在独立文件中尽情书写艺术。

## 4. 工程版本管理与 Git 规范 (Versioning & Git Standards)
为了保证这个即兴艺术长廊在持续开发中的稳定性和工程规范，项目采用**语义化版本（SemVer）**与**约定式提交（Conventional Commits）**来管理所有改动的生命周期：

### 1. 语义化版本号（SemVer）规范
项目版本号格式为 `v[主版本号].[次版本号].[修订号]`（例如：`v1.0.1`）：
- **主版本号 (Major - X)**：发生**架构级的断代重构**（如重构全局生词成就库底层、大规模目录迁移）。
- **次版本号 (Minor - Y)**：**新增了新歌页/新专辑**（如全新设计并上线了 `/song/dakara-boku-wa`）。
- **修订号 (Patch - Z)**：**纯视觉样式调整或小 Bug 修复**（如微调朱红分栏线、修复 Flex 轴向塌陷）。

### 2. Git 提交信息（Commit Message）规范
日常开发提交必须简洁有力，并使用前缀明确标识本次改动的“层级”：
- `feat:` ➡️ 新增功能或歌曲页面（例如：`feat: 上线《所以我放弃了音乐》信笺显影页`）
- `style:` ➡️ 纯视觉样式、配色、动画、布局的微调（例如：`style: 升级古典朱砂分栏线`）
- `fix:` ➡️ 修复渲染崩溃、布局错位等技术 Bug（例如：`fix: 解决竖排模式下 flex 容器塌陷折行 bug`）
- `refactor:` ➡️ 代码重构，如提取公共 React 钩子，不改变外部功能（例如：`refactor: 提取 useSpeech 发音钩子`）
- `chore:` ➡️ 杂事与日常维护。如修改配置、清理项目垃圾、更新忽略规则（例如：`chore: 物理删除 dist 文件夹并从 git 追踪中剥离`）

### 3. 里程碑标记（Git Tag）
每当成功部署或累积完成了一个重要版本节点（如一张新专辑、或者一次完美的样式升级）：
- 在终端运行 `git tag v[X.Y.Z]` 为当前 Commit 打上物理标记（如 `git tag v1.0.1`）。
- 这将使整个项目的成长轨迹拥有极其清晰、可回溯的里程碑记录。

### 4. 提交前防污染审查（.gitignore 拦截机制）⚠️
为了保证 Git 仓库的历史树健康、纯净，且不发生编译冲突：
- **凡是新建文件或运行构建命令时**，AI 必须严格审查所涉及的目录。
- **严禁**将任何本地构建产物（如 `dist/`）、依赖包（如 `node_modules/`）、编辑器缓存（如 `.vscode/`）、系统临时文件或各种调试日志（如 `*.log`）带入 Git 版本库。
- 每次 Commit 前，AI **必须主动检查**并维护 `.gitignore`。若发现有临时或构建产物未被拦截，须立刻将其写入 `.gitignore` 并使用 `git rm --cached` 从缓存中彻底剥离，防微杜渐！


